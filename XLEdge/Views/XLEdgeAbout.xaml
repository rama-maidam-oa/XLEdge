<utils:DpiAwareWindow x:Class="XLEdge.Views.XLEdgeAbout" 
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
					  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                      xmlns:data="clr-namespace:XLEdge" 
                      xmlns:local="clr-namespace:XLEdge.Views"
					  xmlns:utils="clr-namespace:XLEdge.Utilities" 
					  xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.FontAwesome" 
					  WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
					  SizeToContent="Manual" MinWidth="400" MaxWidth="650"
					  MinHeight="450" MaxHeight="600"
					  Background="White" ShowInTaskbar="False">
    <utils:DpiAwareWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/XLEdge;component/Themes/GlobalStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- System Color Overrides for Selection (window-specific) -->
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#E6F3FF"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#F0F8FF"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#212529"/>
            <!-- Simple indeterminate-only ProgressBar template (window-specific) -->
            <Style x:Key="IndeterminateOnlyProgressBar" TargetType="ProgressBar">
                <Setter Property="Background" Value="#E9ECEF"/>
                <Setter Property="Height" Value="12"/>
                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4" SnapsToDevicePixels="True">
                                <Grid ClipToBounds="True" Height="{TemplateBinding Height}">
                                    <!-- Moving stripe -->
                                    <Rectangle x:Name="IndeterminateStripe" Height="{TemplateBinding Height}" Width="160" RadiusX="4" RadiusY="4" HorizontalAlignment="Left" Opacity="1">
                                        <Rectangle.Fill>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Color="#149FDB" Offset="0"/>
                                                <GradientStop Color="#1B6B93" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Rectangle.Fill>
                                        <Rectangle.RenderTransform>
                                            <TranslateTransform x:Name="IndeterminateTransform" X="0"/>
                                        </Rectangle.RenderTransform>
                                    </Rectangle>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsIndeterminate" Value="True">
                                    <Trigger.EnterActions>
                                        <!-- Name the BeginStoryboard so StopStoryboard can target it safely -->
                                        <BeginStoryboard Name="IndeterminateStoryboard">
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetName="IndeterminateTransform" Storyboard.TargetProperty="X" From="-200" To="600" Duration="0:0:1.1"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <!-- Explicitly stop the named storyboard -->
                                        <StopStoryboard BeginStoryboardName="IndeterminateStoryboard"/>
                                    </Trigger.ExitActions>
                                </Trigger>
                                <!-- When not indeterminate, hide stripe so determinate visuals (if any) are not shown incorrectly -->
                                <Trigger Property="IsIndeterminate" Value="False">
                                    <Setter TargetName="IndeterminateStripe" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </utils:DpiAwareWindow.Resources>
    <!-- Bug fix ("all windows: card style not looking good, make it simple like GLSense"): dropped
         the floating-card treatment (transparent window + bleed margin + DropShadowEffect + rounded
         inner card) in favor of a plain, flat, opaque, square-cornered window - see XLEdgeOptions.xaml
         for the full rationale comment. -->
    <Border Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Padding="10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- ================= Header ================= -->
        <Border Grid.Row="0" Style="{StaticResource HeaderBar}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Left side with icon and title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconFontAwesome Kind="CircleInfoSolid" Style="{StaticResource LargeIcon}"/>
                    <TextBlock Text="About Orbit XLEdge" Style="{StaticResource TitleHeaderTextBlock}"/>
                </StackPanel>
                <Button Grid.Column="2" Style="{StaticResource CustomWindowCloseButtonStyle}" Click="BtnClose_Click"/>
            </Grid>
        </Border>
        <!-- ================= Combined Content Section ================= -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0">
            <StackPanel>
                <!-- ================= Company & Product Info Section ================= -->
                <Border Style="{StaticResource ContentBorderStyle}" Margin="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Company Logo Placeholder -->
                        <Border Grid.Column="0" Width="200" Height="100" Background="White" CornerRadius="8" Margin="0,0,16,0">
                            <Image Source="/XLEdge;component/Images/orbit_logo.png" Stretch="Fill"/>
                        </Border>
                        <!-- Product Details -->
                        <StackPanel Grid.Column="1" Orientation="Vertical">
                            <!-- Version -->
                            <TextBlock x:Name="txtVersion" Text="{Binding Source={x:Static data:XLEdgeAppConstants.DefaultVersion}, StringFormat='Version : {0}'}" FontSize="12" FontFamily="Segoe UI" Foreground="#6C757D" Margin="0,0,0,8">
                                <TextBlock.ToolTip>
                                    <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                        <TextBlock Text="Current XLEdge version"/>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                                </TextBlock>
                            <!-- Description -->
                            <TextBlock Text="Financial Analytics and Reporting Tool" FontFamily="Segoe UI" FontSize="12" Foreground="#6C757D" Margin="0,0,0,8">
                                <TextBlock.ToolTip>
                                    <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                        <TextBlock Text="XLEdge: Advanced financial analytics platform"/>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                                </TextBlock>
                            <!-- Build Date -->
                            <TextBlock x:Name="txtBuildDate" FontFamily="Segoe UI" Text="{Binding Source={x:Static data:XLEdgeAppConstants.DefaultCommitDate}, StringFormat='Build Date : {0}'}" FontSize="11" Foreground="#6C757D" Margin="0,0,0,8">
                                <TextBlock.ToolTip>
                                    <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                        <TextBlock Text="Date when this version was built"/>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                                </TextBlock>
                            <!-- Support Link -->
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="For Support please contact : " FontFamily="Segoe UI" FontSize="11" Foreground="#6C757D">
                                    <TextBlock.ToolTip>
                                        <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                            <TextBlock Text="Get help and technical support"/>
                                        </ToolTip>
                                    </TextBlock.ToolTip>
                                    </TextBlock>
                                <TextBlock x:Name="txtSupportLink" Text="https://www.orbitanalytics.com" FontFamily="Segoe UI" FontSize="11" Foreground="#149FDB" TextDecorations="Underline" Cursor="Hand" MouseLeftButtonDown="SupportLink_MouseLeftButtonDown">
                                    <TextBlock.ToolTip>
                                        <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                            <TextBlock Text="Click to open support website"/>
                                        </ToolTip>
                                    </TextBlock.ToolTip>
                                    </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- ================= Instance Compatibility Section ================= -->
                <Border Style="{StaticResource ContentBorderStyle}" Margin="0,0,0,4">
                    <Border.ToolTip>
                        <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                            <TextBlock Text="Shows configured server instances and their compatibility status"/>
                        </ToolTip>
                    </Border.ToolTip>
                    <DataGrid x:Name="dgInstances" FontFamily="Segoe UI" MinHeight="200" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow" Background="White" BorderThickness="0" IsReadOnly="True" VerticalAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <!-- Bug fix ("all windows should be in sync with the styles"): this header style
                             used to be a plain flat Background/Foreground/Border set with no
                             hover/press feedback and no sort-arrow template - unlike the structurally
                             identical grid header in XLEdgeServerConfiguration.xaml, which already has
                             both. Copied that same template here (hover -> #389BC7, pressed -> #277596,
                             an ascending/descending sort-arrow Path) so these two near-twin grids behave
                             and look identical instead of one being visibly less finished than the
                             other. Hardcoded hex values also swapped for the equivalent named resources. -->
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderBrush" Value="#1B6B93"/>
                                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="Height" Value="38"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridColumnHeader">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ContentPresenter Grid.Column="0" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                    <Path x:Name="SortArrow" Grid.Column="1" Margin="6,0,6,0" Width="10" Height="6" Stretch="Fill" Fill="{TemplateBinding Foreground}" Visibility="Collapsed" Data="M 0 4 L 5 0 L 10 4 Z"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" TargetName="border" Value="#389BC7"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" TargetName="border" Value="#277596"/>
                                                </Trigger>
                                                <Trigger Property="SortDirection" Value="Ascending">
                                                    <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                                    <Setter TargetName="SortArrow" Property="Data" Value="M 0 4 L 5 0 L 10 4 Z"/>
                                                </Trigger>
                                                <Trigger Property="SortDirection" Value="Descending">
                                                    <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                                    <Setter TargetName="SortArrow" Property="Data" Value="M 0 0 L 5 4 L 10 0 Z"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="BorderBrush" Value="{StaticResource BackgroundBrush}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Height" Value="36"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F8FF"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E6F3FF"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Instance" Binding="{Binding Instance}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="12,8"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="Foreground" Value="#212529"/>
                                        <Setter Property="TextWrapping" Value="NoWrap"/>
                                        <Setter Property="ToolTip">
                                            <Setter.Value>
                                                <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                                    <TextBlock Text="{Binding Instance}"/>
                                                </ToolTip>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <!-- Compatible Column - ICONS ONLY -->
                            <DataGridTemplateColumn Width="100">
                                <DataGridTemplateColumn.Header>
                                    <Grid>
                                        <TextBlock Text="Compatible" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.ToolTip>
                                                <ToolTip Style="{StaticResource SimpleBrowserToolTip}">
                                                    <TextBlock Text="Compatibility status with current system"/>
                                                </ToolTip>
                                            </TextBlock.ToolTip>
                                        </TextBlock>
                                    </Grid>
                                </DataGridTemplateColumn.Header>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <!-- Green Tick -->
                                            <!-- Bug fix: was #10B981/#EF4444 - a completely separate green/red
                                                 pair from this app's own SuccessBrush/DangerBrush (#28A745/#EC616E),
                                                 used nowhere else. Switched to the shared brushes so
                                                 compatibility status matches every other success/error
                                                 indicator in the app instead of its own one-off palette. -->
                                            <iconPacks:PackIconFontAwesome Kind="CircleCheckSolid" Width="18" Height="18" Foreground="{StaticResource SuccessBrush}">
                                                <iconPacks:PackIconFontAwesome.Style>
                                                    <Style TargetType="iconPacks:PackIconFontAwesome">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCompatible}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </iconPacks:PackIconFontAwesome.Style>
                                            </iconPacks:PackIconFontAwesome>
                                            <!-- Red Cross -->
                                            <iconPacks:PackIconFontAwesome Kind="CircleXmarkSolid" Width="18" Height="18" Foreground="{StaticResource DangerBrush}">
                                                <iconPacks:PackIconFontAwesome.Style>
                                                    <Style TargetType="iconPacks:PackIconFontAwesome">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCompatible}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </iconPacks:PackIconFontAwesome.Style>
                                            </iconPacks:PackIconFontAwesome>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- ================= Progress Bar Section ================= -->
                <Border Background="Transparent" Margin="0,0,0,4">
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center" x:Name="progressPanel" Visibility="Visible">
                        <ProgressBar x:Name="progressBar" Style="{StaticResource IndeterminateOnlyProgressBar}" IsIndeterminate="True" Height="20" Foreground="#149FDB" Background="#E9ECEF" BorderThickness="1" BorderBrush="#DEE2E6"/>
                        <TextBlock x:Name="txtProgress" FontFamily="Segoe UI" Text="Checking instance compatibility..." FontSize="11" Foreground="#6C757D" HorizontalAlignment="Left" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- ================= Footer with Close Button ================= -->
        <Border Grid.Row="2" Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Copyright Text -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Copyright © 2015 Orbit Analytics. All Rights Reserved." FontFamily="Segoe UI" FontSize="12" Foreground="#6C757D"/>
                </StackPanel>
                <!-- Close Button -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="btnClose" Style="{StaticResource CloseButtonStyle}" Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ================= App Overlay ================= -->
        <local:AppOverlay x:Name="AppOverlayControl" Grid.RowSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Collapsed" Panel.ZIndex="10"/>
    </Grid>
    </Border>
</utils:DpiAwareWindow>